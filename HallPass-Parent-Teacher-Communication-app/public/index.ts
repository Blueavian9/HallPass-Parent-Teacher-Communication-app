import settings from "../src/settings.json"; // TypeScript should now resolve this

console.log(settings.debug); // Access the `debug` property
console.log(settings.dry); // Access the `dry` property
